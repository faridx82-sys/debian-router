# debian-router
A script for creating your own router using Debian as base OS.

Installation:

Run install.sh. This will call all the scripts to run one by one starting from installpackage.sh and so on.
